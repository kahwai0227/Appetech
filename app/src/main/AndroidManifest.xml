<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appetech_smart_cafeteria"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".RegistrationPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ArkedList"
            android:label="@string/app_name" />
        <activity
            android:name=".Delete_R"
            android:label="@string/app_name" />
        <activity
            android:name=".Delete_R_success"
            android:label="@string/app_name" />
        <activity
            android:name=".Q_Sure"
            android:label="@string/app_name" />
        <activity
            android:name=".ReservationDetails"
            android:label="@string/app_name" />
        <activity
            android:name=".ReservationForm"
            android:label="@string/app_name" />
        <activity
            android:name=".success_book"
            android:label="@string/app_name" />
    </application>

</manifest>